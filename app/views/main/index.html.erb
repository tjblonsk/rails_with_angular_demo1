<div ng-controller="IndexCtrl">
  <h1 class="text-center">{{ title }}</h1>
    <div class="row" ng-repeat="post in posts">
      <h2>{{ post.title }}</h2>
      <p>{{ post.contents }}</p>
  </div>
</div>

